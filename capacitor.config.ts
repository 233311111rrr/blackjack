import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blackjackpro.advisor',
  appName: 'Blackjack Pro Advisor',
  webDir: 'dist'
};

export default config;
